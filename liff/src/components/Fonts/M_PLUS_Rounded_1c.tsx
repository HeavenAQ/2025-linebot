// Offline-safe fallback: avoid fetching Google Fonts during build.
// If you want the real font, restore next/font/google usage.
export const mPlusRounded1c: { className: string } = { className: '' }
